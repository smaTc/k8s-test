apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bootcamp-ingress
spec:
  rules:
    - http:
        paths:
          - pathType: Prefix
            path: /api
            backend:
              service:
                name: bootcamp-service
                port:
                  number: 8080